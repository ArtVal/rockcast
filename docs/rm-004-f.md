# RM-004-F — RockCast shared station catalog integration

Status: RM-004-I cutover complete locally (2026-08-25); acceptance checks recorded below.

RockCast vendors the approved RM-004-C schema-v1 release candidate as
assets/catalog/stations.v1.json with assets/catalog/manifest.json.

- catalogVersion: 2026.08.2
- schemaVersion: 1
- SHA-256: 3fa20dca94fc059bd433a47b9fba9bb6d5e5e1aa2957a5ffb58b2a7b20b1d74d

The local loader verifies the manifest, catalogVersion, and canonical UTF-8/LF SHA-256 before
parsing the bundled snapshot. It never fetches a catalog at build or startup and does not use the
mobile extended SQLite catalog.

JSON overrides preserve the established precedence: ROCKCAST_STATIONS, executable directory,
working directory, then app-data directory. At each directory source, stations.v1.json takes
precedence over stations.txt. schema-v1 stations use their explicit stable IDs and primary stream
for the unchanged Station.url playback, relay, and Cast path; all streams remain on Station metadata.

The stations.txt adapter preserves existing user overrides during exactly one schema-v1 release
cycle. It is the sole retained legacy exception at cutover: **owner: RockCast maintainers; reason:
protect existing offline user overrides; removal date: 2026-10-31, after the first schema-v1
release cycle completes.** Do not remove it before that date. RM-004-I records the exception rather
than extending or silently replacing it.

RM-004-G updates the vendored JSON/schema/manifest only with the explicit offline
`release_sync.py sync rockcast ... --release <immutable-dir>` command. Use dry-run, review, then
verify; a corrupt or mismatched release is rejected. Neither build nor startup downloads a catalog.
No manual catalog-copy workflow is supported. A rollback selects a prior immutable release through
the same tool; the bundled verified baseline remains available when RockServer is stopped.
